/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // reactCompiler: false,
  // reactStrictMode: false,

  "devIndicators": false,
};

export default nextConfig;
